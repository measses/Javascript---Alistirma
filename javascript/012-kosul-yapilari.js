// Bu ders notunda koşul yapılarından bahsedilecektir.
//prompt ile kullanıcıdan veri alma

/*let not = prompt("Notunuzu giriniz: ");

if (not>50) {
    console.log("Geçtiniz , Notunuz: " + not);
} else {
    console.log("Kaldınız, Notunuz: " + not);
} */

//vize final hesaplama

/*let vize = Number(prompt("Vize notunuzu giriniz: "));
let final = Number(prompt("Final notunuzu giriniz: "));

if (vize >= 0 && vize <= 100 && final >= 0 && final <= 100) {
    let ortalama = vize * 0.4 + final * 0.6;
    if (ortalama >= 50 && final >= 60) {
        console.log("Geçtiniz, Ortalamanız: " + ortalama);
    } else {
        console.log("Kaldınız, Ortalamanız: " + ortalama);
    }
}
*/
